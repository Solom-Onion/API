# Mongo URI, always starts with mongodb://HOST:IP
mongo_uri=mongodb://localhost:27017

# Server port, you already know
server_port=8080

# Mongo Database
database=solomon_dev

# Mongo Collection
collection=database

# Limit for database results
results_limit=15